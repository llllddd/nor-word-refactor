{
  "name": "word-refactor-update",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:debug": "tsc && vite build --mode debug",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "chrome-types": "^0.1.425",
    "esbuild": "^0.27.4",
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  }
}
